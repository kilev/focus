-в файл input.txt(который находится в папке ресурс) первыми двумя строками вводить тип фигуры и ее параметры через пробел.
-В данной версии читает из файла только одну фигуру(верхнюю), остальное игнорит.
-вывод происходит в консоль.
-логи пишутся в figures.log
